# 리듬게임 구현 프롬프트 (Claude Code용)

> 아래 전체를 Claude Code에 붙여넣으세요. 프로젝트 루트에 `SPEC.md`로 저장해두면 이후 세션에서도 참조됩니다.

---

수직낙하형 리듬게임을 만들어줘. 아래 사양을 그대로 따르고, **마일스톤 단위로 끊어서 작업한 뒤 매번 실행 가능한 상태로 만들고 나에게 확인받아줘.** 한 번에 전부 구현하지 마.

## 1. 기술 스택 / 제약

- Vite + TypeScript + Canvas 2D (프레임워크 없이). 오디오는 Web Audio API.
- 외부 게임 엔진/리듬게임 라이브러리 사용 금지. 렌더링과 타이밍 로직은 직접 구현.
- 브라우저 단독 실행 (백엔드 없음). 채보/음원은 로컬 파일에서 로드.
- **로컬 스탠드얼론 빌드 가능성을 열어둔다.** 브라우저 환경에서 오디오/입력 오프셋 보정이 실사용 중 부족하다고 판명되면(OS/브라우저 스케줄링발 지터 등), Tauri 등으로 동일한 Vite 앱을 감싸는 로컬 실행형 빌드를 추가하는 것을 고려한다. 지금 당장 구현하지는 않으며, 마일스톤 8(오프셋 옵션) 이후 실제 플레이 테스트에서 필요성이 확인되면 별도 마일스톤으로 다룬다. 게임 로직/렌더링 코드는 브라우저 종속 API에 최대한 얇게 의존하도록 유지해 이 전환 비용을 낮춘다.
- **타이밍 정확도가 이 프로젝트의 핵심이다:**
  - 게임 시간 기준은 반드시 `AudioContext.currentTime`. `requestAnimationFrame`의 timestamp나 `Date.now()`를 게임 클럭으로 쓰지 말 것.
  - `rAF`는 렌더링 전용. 판정은 입력 이벤트 발생 시점에 즉시 계산.
  - 키 입력 시각은 `KeyboardEvent.timeStamp`를 사용해 프레임 지터를 제거하고, 오디오 클럭으로 변환해서 판정할 것.
  - 사용자 조정 가능한 오디오/입력 오프셋(ms) 설정을 제공.
  - (2026-08-08 결정) 게임 화면의 TIME 표시는 경과 시간 카운트업이 아니라 **곡이 끝날 때까지
    남은 시간을 보여주는 카운트다운**이다. 총 길이는 판정 대상 마지막 노트 시각(홀드는 종료
    시각) + 자동 MISS 윈도우로 계산한다(`chartDurationMs`, `isChartComplete`와 동일 기준).

## 2. 레인 구성 / 입력

원핸드 조작 전제. **모드는 3레인 + FX 하나로 통일한다. 4키 모드는 만들지 마.**

| 입력 | 대상 |
|---|---|
| A, S, D | 노트 레인 0, 1, 2 |
| Space | FX 노트 |
| 마우스 상하 | 스크래치 레인 |

### FX 노트

- FX는 **별도의 세로 레인이 아니다.** 3개 노트 레인 전체를 가로로 덮는 긴 바 형태의 노트다. (사운드볼텍스 FX, DJMAX 8B FX 노트 참고)
- 렌더링 시 **노트 레인보다 아래 레이어**에 그린다. 즉 FX 바가 먼저 그려지고 그 위에 일반 노트가 겹쳐 그려져서, 동시에 떨어져도 일반 노트가 가려지지 않아야 한다.
- FX 바는 반투명 + 노트 레인과 확실히 구분되는 색상. 폭은 3레인 전체 + 약간의 여백.
- FX도 탭(칩)과 롱(홀드) 두 종류를 지원한다.
- 판정은 일반 노트와 완전히 독립. 같은 시각에 A + FX가 겹치면 두 개의 별도 판정이 발생한다.

- 키 리매핑 가능하게 설계 (설정에서 변경).
- 키 반복(auto-repeat) 무시. `keydown` 1회당 1입력.
- 여러 레인 동시입력 정상 처리.

### 스크래치 레인 (마우스)

- 화면 **우측이 기본값**(오른손 마우스 사용자가 다수이므로), 설정에서 좌측으로 전환 가능.
- 스크래치 레인 폭은 **노트 레인 1개 폭 + 20px**로 계산한다. 캔버스 폭이 바뀌어도(6절 참고) 이 관계는 유지된다: `NOTE_LANE_WIDTH = (CANVAS_WIDTH - 20) / (레인수 + 1)`, `SCRATCH_LANE_WIDTH = NOTE_LANE_WIDTH + 20`.
- 입력은 `mousemove`의 `movementY` 누적. Pointer Lock API를 써서 커서가 화면을 벗어나지 않게 할 것.
- `SCRATCH_THRESHOLD`(누적 이동량, 설정 가능)를 넘으면 해당 방향(UP/DOWN)의 스크래치 입력 1회로 인정. 인정 즉시 누적값 리셋.
- **방향 규칙 (중요):**
  - `lastDirection`을 저장한다. 스크래치 입력은 **방향이 `lastDirection`과 다를 때만** 유효 입력으로 인정된다.
  - 유효 입력이 발생하면 `lastDirection = 해당 방향`, `lastScratchTime = 현재 시각`.
  - **마지막 유효 입력으로부터 2초가 경과하면 `lastDirection`을 `null`로 리셋한다.** 리셋 후에는 직전과 같은 방향이어도 다시 유효 입력이 된다.
  - 즉 연속 스크래치는 방향을 번갈아 처리해야 하지만, 2초 이상 간격이 벌어지면 같은 방향으로 계속 처리할 수 있다.
  - 2초 값은 상수로 분리(`SCRATCH_DIR_RESET_MS = 2000`).
- 무효 입력(같은 방향 연속, 2초 미경과)은 판정을 발생시키지 않고 조용히 무시. 노트를 소모하지도 않는다.

## 3. 노트 / 스크롤

- 수직낙하형. 노트는 위에서 아래로 떨어지고 하단 고정 판정선에서 처리 (DJMAX, beatmania IIDX 스타일).
- 노트 타입: 일반 노트(탭/홀드), FX 노트(탭/홀드), 스크래치 노트.
  - **홀드는 판정을 1회만 받는다** (시작 시점, 일반 노트와 동일한 판정 테이블/윈도우 사용). 떼는 시점의 판정은 없다.
  - 홀드를 누르고 있는 동안 일정 간격으로 **틱**이 발생해 "처리 중"임을 시각적으로 보여준다. 틱은 **콤보만 증가**시키고 점수/정확도에는 반영되지 않는다.
  - 틱 간격은 기본적으로 BPM에 연동(`holdTickIntervalBeats`, 기본 1비트)되며, 채보 JSON에서 채보 전체 기본값 또는 노트별로 오버라이드해 제작 툴에서 커스텀할 수 있다.
  - 키를 도중에 떼면 그 순간부터 틱이 멈추고, **재개되지 않는다**(다시 눌러도 이어지지 않음). 이때도 별도의 MISS 판정은 발생하지 않는다.
  - **스크래치 롱노트는 구현하지 않는다.** 탭과 홀드가 시각/조작적으로 구분이 모호해지므로 스크래치는 탭만 지원 (`parseChart`가 스크래치+hold 조합을 파싱 단계에서 거부).
- 렌더 순서(아래 → 위): 레인 배경 → FX 노트 → 일반 노트 → 판정선 → 이펙트.
- 스크롤 속도는 **그린넘버 방식**: 노트가 화면 상단에서 판정선까지 도달하는 시간(ms)을 기준으로 계산. BPM이 바뀌어도 체감 속도가 유지되도록 한다.

## 4. 판정

각 등급의 ms 값은 **패스트 쪽·슬로우 쪽 각각의 한계값**이다. 즉 윈도우는 노트 시각을 중심으로 좌우 대칭이며, 실제 폭은 표기값의 2배다.

**판정 테이블은 노트용과 스크래치용 두 벌이 존재한다.** 판정 함수는 노트 타입에 따라 다른 테이블을 참조하도록 설계할 것.

### 노트 판정 (A/S/D 및 FX)

| 판정 | 패스트 | 슬로우 | 점수 |
|---|---|---|---|
| PERFECT+ | −20 ms | +20 ms | 4 |
| PERFECT | −40 ms | +40 ms | 3 |
| GREAT | −60 ms | +60 ms | 2 |
| GOOD | −80 ms | +80 ms | 1 |
| MISS | 그 밖 또는 미입력 | | 0 |

### 스크래치 판정

마우스는 폴링 레이트와 임계값 누적 때문에 입력 지연 편차가 키보드보다 구조적으로 크다. 이를 보정하기 위해 **PERFECT+ 를 40ms까지 확장하고 PERFECT 등급을 없앤다.**

| 판정 | 패스트 | 슬로우 | 점수 |
|---|---|---|---|
| PERFECT+ | −40 ms | +40 ms | 4 |
| GREAT | −60 ms | +60 ms | 2 |
| GOOD | −80 ms | +80 ms | 1 |
| MISS | 그 밖 또는 미입력 | | 0 |

- 스크래치에는 3점(PERFECT) 등급이 존재하지 않는다. 40ms를 벗어나면 곧바로 2점으로 떨어진다. 이는 의도된 설계다.
- 이론치 계산은 동일하게 노트 1개당 4점이므로, 스크래치가 섞여도 정확도 산식은 바뀌지 않는다.

### 공통 규칙

- 판정 함수는 절대 오차 `|입력시각 − 노트시각|`로 등급을 정하고, 오차의 **부호**를 별도로 반환한다 (음수 = FAST, 양수 = SLOW).
- 등급 경계는 이하(≤) 포함. 즉 노트에서 오차 40ms는 PERFECT, 41ms는 GREAT.
- 노트가 슬로우 80ms를 지나가면 자동 MISS 처리.
- 두 판정 테이블 모두 상수로 분리하고 설정에서 노출. 스크래치 테이블은 실제 플레이하며 조정할 가능성이 높으므로 특히 쉽게 바꿀 수 있어야 한다.

### FAST / SLOW 표시

- 판정 텍스트 아래에 FAST 또는 SLOW를 표시. PERFECT+ 는 표시하지 않는다(정타 취급).
- 스크래치는 PERFECT+ 범위가 40ms이므로 그 안에서는 FAST/SLOW가 뜨지 않는다.
- 화면에 누적 FAST 카운트 / SLOW 카운트를 상시 표시.

### 판정바 (타이밍 편차 바)

- 판정선 아래(위치 설정 가능)에 가로형 판정바를 배치.
- 중앙이 오차 0, 좌측이 FAST, 우측이 SLOW. 바의 표시 범위는 ±80ms (GOOD 경계까지).
- 노트를 칠 때마다 해당 오차 위치에 눈금(tick)을 찍고, 일정 시간(기본 2초)에 걸쳐 서서히 사라지게 한다. 최근 N개(기본 30개)만 유지.
- 눈금 색상은 판정 등급 색상과 동일하게. 스크래치와 키 입력 눈금은 형태 구분 없이 동일한 막대로 표시한다(초기에는 마름모로 구분했으나 단순한 막대가 더 보기 좋다는 피드백으로 통일).
- 바 배경에 각 판정 윈도우 구간을 옅은 색 띠로 깔아서 20/40/60/80ms 경계가 보이게 할 것.
- 최근 입력들의 **평균 오차**를 바 위에 별도 마커로 표시. 이 값이 한쪽으로 치우쳐 있으면 오디오/입력 오프셋을 조정하라는 힌트를 띄운다. 키 입력과 스크래치의 평균 오차는 따로 집계할 것 — 두 입력 경로의 지연이 다르기 때문이다.
- 판정바 on/off 및 표시 위치는 옵션에서 설정.

- 그 외 표시: 판정별 카운트, 최대/현재 콤보.

## 5. 점수

- **상한선 없는 누적 방식.** 1000만점제 같은 정규화 금지.
- `SCORE = Σ(판정 점수)` — PERFECT+ 4, PERFECT 3, GREAT 2, GOOD 1, MISS 0.
- 롱노트는 시작 판정 1회만 채점 대상이다(일반 노트와 동일한 가중치). 유지 중 틱은 콤보만 올리고 점수에는 포함되지 않는다.
- 이론치 = (판정 대상 총 개수 — 롱노트는 1개로 카운트) × 4. 정확도(%) = SCORE / 이론치 × 100 으로 함께 표시.
- 결과 화면: 총점, 이론치, 정확도, 판정별 개수, 맥스콤보, FAST/SLOW 집계, 판정 오차 분포 그래프.
  - (2026-08-08 결정) 오차 분포 그래프는 연속 히스토그램 대신 **판정 종류와 통일된 막대그래프**로 그린다:
    가운데 PERFECT+(노란색), 좌우로 대칭으로 PERFECT/GREAT/GOOD/MISS가 각 판정 색 그대로 뻗어나가고,
    막대 아래에 판정 이름을 표기한다. MISS는 자동 미스만 존재해 방향 정보가 없으므로(SPEC.md 3절)
    FAST/SLOW MISS 막대는 게임 메커니즘상 항상 0이다.

## 6. 게임 옵션

플레이 시작 전 옵션 화면에서 설정. 모두 채보 원본을 변형하지 않고 런타임에 적용한다.

- **속도**: 배속(0.5~10.0, 0.25 단위) + 그린넘버 직접 입력. 내부적으로는 `effectiveGreenNumberMs` 하나만 진짜 상태이며, 배속과 그린넘버는 이 값을 조작하는 두 가지 입력 경로일 뿐이다.
  - `effectiveGreenNumberMs = baseGreenNumberMs / speedMultiplier` (기준: 배속 1.0x = `baseGreenNumberMs`, 기본값 800ms)
  - 배속을 입력하면 그린넘버가 위 공식으로 재계산되어 함께 표시된다.
  - 그린넘버를 직접 입력하면 그 값이 `effectiveGreenNumberMs`에 바로 반영되고, 배속 표시값은 `baseGreenNumberMs / effectiveGreenNumberMs`로 역산되어 함께 표시된다.
  - 플레이 중 단축키로 실시간 변경.
- **캔버스 폭**: 좁게 / 보통(기본값) / 넓게 3단계. 보통을 기준으로 좁게/넓게는 각각 ∓30%.
  - 보통 480px 기준: 좁게 336px, 넓게 624px. 높이는 800px로 고정, 폭만 변경.
  - `NOTE_LANE_WIDTH`/`SCRATCH_LANE_WIDTH`는 선택된 폭으로부터 위 스크래치 레인 공식으로 매번 유도.
- **노트 배치**: MIRROR ON/OFF 토글. (2026-08-08 결정: 3레인짜리 원핸드 게임에서는 RANDOM 계열의 의미가 작아 RANDOM/S-RANDOM/R-RANDOM은 구현하지 않는다.) **컨트롤 위치는 옵션 화면이 아니라 선곡 팝업**(6-1절)이다 — 곡을 고르는 시점에 바로 켜고 끌 수 있는 게 더 자연스럽다는 판단.
  - 배치 옵션의 대상은 **노트 레인 0~2 뿐이다.** FX와 스크래치는 항상 제외.
  - MIRROR는 0↔2 스왑.
- **시야(SUDDEN+/HIDDEN+/LIFT)는 구현하지 않기로 함(2026-08-08 결정).**
- **기타**: 오디오 오프셋, 입력 오프셋, 판정선 위치, 노트 스킨 색상(자유 색상 선택 대신 가독성 검증된 팔레트 5종 중 선택), 마우스 감도(스크래치 임계값 `SCRATCH_THRESHOLD`, 2절에서 이미 "설정 가능"으로 명시됨). **오토 스크래치는 구현하지 않기로 함(2026-08-08 결정).**
- **프리셋 3개**를 localStorage에 저장/복원한다(2026-08-08 결정: 자동 저장 하나가 아니라 사용자가 원하는 슬롯에 명시적으로 저장). 프리셋 버튼으로 슬롯을 전환하면 그 슬롯에 저장된 값(없으면 기본값)이 옵션 화면에 즉시 반영되고, "이 프리셋에 저장" 버튼을 눌러야 지금 화면의 값이 그 슬롯에 저장된다.
- **메트로놈 클릭(더미 채보 검증용)은 구현하지 않는다.** 플레이 환경(오디오 출력 지연 등)마다 다르게 동작할 수 있어 오히려 혼란을 줄 수 있음 — 시각적 검증만으로 충분.

### 6-1. 화면 흐름 / 선곡 화면 (2026-08-08 추가)

옵션 화면은 더 이상 앱 시작 시 처음 보이는 화면이 아니다. **선곡 화면이 진입점**이 되고, 옵션은
선곡 화면에서 스페이스바를 누르면 나타나는 모달로 바뀐다.

- **선곡 화면**("SELECT"): 악곡 목록(이미지 + 제목 + 아티스트 + EASY/NORMAL/HARD 난이도, 난이도는
  세로로 쌓지 않고 3블록을 가로로 나열해 오른쪽 끝에 꽉 채운다)을 보여준다. 최소 1:1 이상의
  가로형 레이아웃이며(게임플레이 캔버스의 세로 비율을 그대로 물려받지 않는다), 뷰포트보다 커지지
  않도록 상한을 둬서 페이지 스크롤이 생기지 않는다 — 곡이 많아 넘치는 부분은 목록 내부 스크롤만
  움직인다.
  - 지금은 테스트용 더미 채보 기반 곡 3개가 등록되어 있다(리스트-팝업 바인딩 확인용). 난이도 3개는
    표시/선택만 될 뿐 아직 서로 다른 채보를 가리키지 않는다(추후 실제 채보가 늘어나면 난이도별로
    다른 채보를 연결할 지점).
  - 곡을 클릭하면 화면 아래에서 위로 팝업(높이 = 화면 영역의 4/10)이 올라온다. 팝업에는 앨범아트,
    제목, 아티스트, 난이도 선택, MIRROR ON/OFF 토글, "START" 버튼이 있다.
  - 팝업은 Esc 또는 팝업/곡 목록 바깥 클릭 시 닫힌다. "START"를 누르면 게임 화면으로 전환되어
    플레이가 시작된다.
- **옵션 화면**: 선곡 화면에서 스페이스바를 누르면 모달로 뜨고, 다시 스페이스바를 누르거나 배경을
  클릭하거나 "닫기"를 누르면 닫힌다. 닫힐 때 화면에 있는 값이 실제 런타임 설정에 반영된다.
- **결과 화면**: "다시하기"(같은 곡 재시작) / "SELECT"(선곡 화면으로 돌아가기) 두 버튼을 둔다.

## 7. 채보 포맷

JSON. 스키마를 먼저 정의하고 타입으로 강제할 것.

```jsonc
{
  "title": "...",
  "artist": "...",
  "audio": "song.ogg",
  "offset": 0,          // ms, 오디오 시작과 0박 사이 보정
  "bpmChanges": [{ "time": 0, "bpm": 150 }],
  "level": 12,
  "holdTickIntervalBeats": 1,   // 선택. 홀드 틱 간격(비트 단위) 채보 전체 기본값. 생략 시 config.ts 기본값 사용
  "notes": [
    { "time": 1200, "lane": 0, "type": "tap" },
    { "time": 1400, "lane": 2, "type": "hold", "duration": 500 },
    { "time": 1600, "lane": "fx", "type": "tap" },
    { "time": 1800, "lane": "fx", "type": "hold", "duration": 800, "tickIntervalBeats": 0.5 }, // 선택, 노트별 오버라이드
    { "time": 2000, "lane": "scratch", "type": "tap" }
  ]
}
```

- 홀드 틱 간격은 `노트별 tickIntervalBeats` > `채보 전체 holdTickIntervalBeats` > `config.ts` 기본값 순으로 적용.
- 테스트용 더미 채보 1개를 생성해서 음원 없이도 동작 검증이 가능하게 할 것 (메트로놈 클릭으로 대체).
- BMS/VOS 등 외부 포맷 임포트는 이번 범위 밖.

## 8. 구현 순서 (마일스톤)

각 단계 끝에서 실행 가능한 상태로 만들고, 무엇을 확인하면 되는지 알려준 뒤 멈춰. 다음 단계로 넘어가기 전에 내 확인을 받아.

1. **프로젝트 셋업 + 오디오 클럭** — Vite/TS 구성, `AudioContext` 기반 게임 클럭, 화면에 경과 시간과 BPM 표시.
2. **렌더링** — 레인/판정선/노트 낙하 렌더링. 더미 채보로 노트가 정확한 타이밍에 판정선에 도달하는지 시각 검증.
3. **키 입력 + 판정** — A/S/D 입력, 판정 윈도우 5단계, 콤보, FAST/SLOW 표시, 판정바.
4. **점수/결과 화면** — 누적 점수, 이론치, 정확도, 판정별 집계.
5. **스크래치 레인** — Pointer Lock, `movementY` 누적, 임계값, 방향 교대 규칙 + 2초 리셋, 스크래치 전용 판정 테이블 적용. **이 단계는 유닛 테스트를 반드시 작성할 것** (아래 참조).
6. **롱노트** — 시작/종료 판정, 홀드 중 이탈 처리.
7. **FX 노트** — 3레인 전체를 덮는 가로 바, 밑 레이어 렌더링, 탭/홀드, 일반 노트와 독립 판정. 일반 노트와 동시에 떨어질 때 가려짐이 없는지 반드시 시각 확인.
8. **옵션 시스템** — 속도, 정배/미러, 오프셋/판정선 위치/노트 색, 저장/복원. (RANDOM 계열, SUDDEN+/HIDDEN+/LIFT, 오토 스크래치는 6절 결정에 따라 범위 밖.)
9. **폴리싱** — 노트 히트 이펙트, 판정 텍스트 애니메이션, 키빔, 게이지, 일시정지.
   - **게이지**는 아래 8-1절 사양대로 단계별 구현(9-1 참조): ① 핵심 로직 ② 옵션 연동 ③ 게임플레이 연결 ④ HUD 렌더링 ⑤ 실패 처리 ⑥ 클리어 판정 + 결과.

## 8-1. 게이지 시스템 (2026-08-09 결정)

BEATMANIA IIDX의 게이지 구조(증감량 계산, 서바이벌/그루브 이원화)를 베이스로, SOUND VOLTEX의 게이지 특성(0% 시작·70% 보더, 롱노트 틱 증감, 폭사 후 구제 옵션)을 혼합한다. 사용자가 옵션 화면에서 곡마다 선택.

### 게이지 종류

| 게이지 | 타입 | 시작 | 클리어 조건 | 하한 |
|---|---|---|---|---|
| NORMAL | 그루브 | 0% | 종료 시점 70% 이상 | 0% (폭사 없음, 색: 70% 전 초록/후 분홍) |
| HARD | 서바이벌 | 100% | 0% 도달 없이 완주 | 0%(폭사), 색: 빨강 |
| CHALLENGE | 서바이벌 | 100% | 0% 도달 없이 완주 | 0%(폭사), 색: 보라 |

### 판정별 증감량

| 판정 | NORMAL | HARD | CHALLENGE |
|---|---|---|---|
| 퍼펙+ / 퍼펙 | +a | +0.16% | +0.16% |
| 그레잇 | +a × 0.75 | +0.08% | +0.08% |
| 굿 | +a × 0.5 | 0 | 0 |
| 미스 | −4.5% | −9.0% | −18.0% |

퍼펙+와 퍼펙의 게이지 증가량이 같은 것은 의도된 사양이다(차등은 점수 4점/3점에서만 둔다).

`a`(%)는 채보 로드 시 총 판정 대상 노트 수 `n`으로 1회 산출:
- `n < 350`: `a = 266.67 / n`
- `n ≥ 350`: `a = 800 / (n + 700)`

FX 롱노트는 시작 판정으로 1회 증감 후, 유지 구간 동안 틱마다 판정한다. 유지 중이면 틱당 `+a × 0.5`(서바이벌형은 +0.08%), 끊긴 상태면 틱당 해당 타입 미스 감소량의 1/4 고정(저체력 보정 무관): NORMAL −1.125% / HARD −2.25% / CHALLENGE −4.5%.

### 부속 규칙
- 게이지 값은 내부 실수 유지, UI는 1% 단위로 버림 표시. 상한 100% 클램프(초과분 이월 없음).
- 스크래치 노트는 판정 폭만 다를 뿐 게이지 증감 규칙은 일반 노트와 동일.
- **HARD/CHALLENGE 저체력 보정**: HARD는 잔량 30% 이하일 때 미스 감소량이 절반(−4.5%)로 줄어든다. CHALLENGE에는 이 보정이 없다. (30% 이하 경고 연출은 구현하지 않는다.)
- 감소 후 잔량이 2% 미만이면 0%로 처리하고 즉시 연주를 중단한다(서바이벌형 폭사).
- **HARD/CHALLENGE 폭사 시 화면**: 레인 위에서부터 가운데에 "FAILED" 글자가 크게 새겨진 셔터를 내리는 연출.

### GAS (Gauge Assist System) — 기본 OFF, HARD/CHALLENGE 전용
폭사 방지 옵션. 옵션창에서 게이지 선택 바로 아래에 위치하며 HARD/CHALLENGE를 선택했을 때만 노출(NORMAL 선택 시 숨김). GAS 라벨 옆 🛈 아이콘에 마우스를 올리면 풀네임과 한줄 설명이 마우스 커서 위치에 종속된 팝업으로 표시된다.
- 켜져 있으면 곡 시작부터 NORMAL 게이지를 백그라운드에서 병행 계산한다. 모든 판정이 표면(HARD/CHALLENGE)과 백그라운드(NORMAL)에 각자의 규칙으로 동시 반영.
- 표면이 0%에 도달하면 연주를 중단하지 않고, 그 시점의 백그라운드 NORMAL 잔량을 그대로 이어받아 표면으로 전환한다(0%에서 재시작하지 않음 — 곡 후반 폭사 시에도 구조적으로 클리어 가능해야 하므로).
- 전환 이후에는 NORMAL 규칙만 적용, 서바이벌 계산 중단. 종료 시점 잔량 70% 이상이면 클리어로 처리하되 등급은 NORMAL 기준으로만 인정.
- 전환 순간 게이지 바 자체가 뒤집히며 NORMAL 게이지로 바뀌는 연출로 피드백한다(색상 반전이 아니라 바가 뒤집히는 전환 애니메이션).
- 코드상 식별자는 동작을 서술하는 이름(`BackupGaugeRelay`)을 쓰고, 플레이어 노출 문자열만 GAS로 표기.
- GAS로 구제된 플레이는 HARD/CHALLENGE 마크를 획득할 수 없다.

### 결과 등급
FAILED / CLEAR / HARD CLEAR / CHALLENGE CLEAR / FULL COMBO / PERFECT. 풀콤보는 게이지 타입과 무관하게 전 노트를 굿 이상으로 처리하면 성립. 곡+난이도+게이지타입별 클리어 마크를 저장하고 선곡 리스트/결과창에 표시(추후 단계).

### 구현 요구사항
- 게이지 계산은 순수 함수 모듈(`src/core/gauge.ts`)로 분리, 렌더링/사운드/입력과 분리.
- 게이지 상태는 복수 인스턴스를 동시에 굴릴 수 있는 구조(GAS의 표면/백그라운드 병행 계산).
- 게이지 타입별 상수는 하나의 테이블 객체(`GAUGE_TYPE_CONFIG`, `config.ts`)로 선언.
- 단위 테스트 필수(`src/core/gauge.test.ts`): HARD 100%에서 연속 미스 14회 폭사(8회에 28%, 이후 저체력 보정), CHALLENGE 6회 폭사, NORMAL 1000노트 전 퍼펙+ 시 100% 클램프, NORMAL 1000노트 미스 80개까지 클리어/81개부터 실패, GAS 전환 시 표면이 백그라운드 잔량 승계, 곡 후반 폭사도 GAS 전환 후 클리어 가능.

### 향후 구현 예정 (범위 밖)
- NORMAL 게이지가 보더(70%)를 넘어선 구간의 시각적 구분 연출(색상/이펙트 전환, 보더 통과 피드백, 원복 처리).
- 게이지 추이 그래프(결과 화면용) 기록.

## 9. 테스트 요구사항

Vitest로 로직 유닛 테스트를 작성해. 특히:

- 노트 판정 함수: 각 경계값(19/20/21, 39/40/41, 59/60/61, 79/80/81 ms)에서 정확한 등급이 나오는지. 패스트(음수) 쪽도 동일하게 대칭 동작하는지.
- 스크래치 판정 함수: 오차 30ms가 PERFECT+(4점)인지, 41ms가 GREAT(2점)인지, 스크래치에서 3점이 절대 나오지 않는지.
- 두 테이블이 섞이지 않는지: 동일한 오차 30ms가 노트에서는 PERFECT(3점), 스크래치에서는 PERFECT+(4점)로 나오는지.
- FAST/SLOW 부호 판별: 오차 −30ms는 FAST, +30ms는 SLOW, 0ms는 어느 쪽도 아님. 단 스크래치 −30ms는 PERFECT+ 범위라 표시하지 않음.
- 점수 계산: 판정 조합에 대한 총점과 정확도.
- 스크래치 방향 상태 머신:
  - UP → UP (0.5초 간격) = 두 번째 무효
  - UP → DOWN = 둘 다 유효
  - UP → (2.5초 대기) → UP = 둘 다 유효
  - UP → DOWN → (2.5초 대기) → DOWN = 세 번 모두 유효
- MIRROR: 0↔2 스왑이 유실/중복 없이 이뤄지는지, FX·스크래치 노트가 변환에서 제외되는지.
- FX 판정이 같은 시각의 일반 노트 판정과 서로 간섭하지 않는지.

## 10. 코딩 규칙

- 게임 로직(판정, 점수, 옵션 변환, 스크래치 상태 머신)은 렌더링/DOM과 완전히 분리해서 순수 함수 또는 순수 클래스로 작성. 테스트 가능해야 한다.
- 매직넘버 금지. 판정 윈도우, 점수, 스크래치 임계값/리셋 시간은 전부 `config.ts`에 상수로.
- 주석과 커밋 메시지는 한국어.
- 각 마일스톤마다 커밋을 나눠서.

---

먼저 마일스톤 1만 구현하고 멈춰줘.
